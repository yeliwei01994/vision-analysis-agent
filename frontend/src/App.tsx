import { useEffect, useMemo, useState } from 'react';
import { api } from './api/client';
import { JobsPage, ModelsPage, RulesPage } from './features/WorkspacePages';
import { detectionSummary, displayEventType, fallbackAnalysis, groupEvents, preciseTime } from './features/eventPresentation';
import type { Detection, EventItem, EventRule, VideoJob } from './types/events';
import './styles.css';

const statusLabel = (s: EventItem['status']) => s === 'confirmed' ? '已确认' : s === 'ignored' ? '已忽略' : '待复核';
const time = (ms: number) => `${Math.floor(ms / 60000).toString().padStart(2, '0')}:${Math.floor(ms / 1000 % 60).toString().padStart(2, '0')}`;
const box = ([x, y, w, h]: Detection['bbox']) => ({ left: `${x}%`, top: `${y}%`, width: `${w}%`, height: `${h}%` });

export default function App() {
  const [events, setEvents] = useState<EventItem[]>([]), [selected, setSelected] = useState<EventItem | null>(null), [frame, setFrame] = useState(0);
  const [rules, setRules] = useState<EventRule[]>([]), [jobs, setJobs] = useState<VideoJob[]>([]), [activeNav, setActiveNav] = useState('事件检索');
  const [keyword, setKeyword] = useState(''), [reviewing, setReviewing] = useState(false), [deleting, setDeleting] = useState<EventItem | null>(null), [error, setError] = useState(''), [feedback, setFeedback] = useState('');
  useEffect(() => { Promise.all([api.listEvents(), api.listRules(), api.listJobs()]).then(([e, r, j]) => { setEvents(e); setSelected(e[0] ?? null); setRules(r); setJobs(j); }).catch(e => setError(e instanceof Error ? e.message : '初始化数据失败')); }, []);
  const groups = useMemo(() => groupEvents(events.filter(e => !keyword || `${e.event_type} ${e.analysis?.summary ?? ''}`.includes(keyword))), [events, keyword]);
  const frames = selected?.evidence.frames ?? [], active = frames[frame] ?? frames[0], analysis = selected ? selected.analysis ?? fallbackAnalysis(selected) : null;
  const choose = (event: EventItem) => { setSelected(event); setFrame(0); };
  async function review(action: 'confirm' | 'ignore') { if (!selected) return; setReviewing(true); setError(''); setFeedback(''); try { const updated = action === 'confirm' ? await api.confirmEvent(selected.id) : await api.ignoreEvent(selected.id); setEvents(items => items.map(i => i.id === updated.id ? updated : i)); setSelected(updated); setFeedback(action === 'confirm' ? '事件已确认' : '事件已忽略'); } catch (e) { setError(e instanceof Error ? e.message : '事件状态更新失败'); } finally { setReviewing(false); } }
  async function remove() { if (!deleting) return; try { await api.deleteEvent(deleting.id); setEvents(items => items.filter(i => i.id !== deleting.id)); setSelected(i => i?.id === deleting.id ? null : i); setDeleting(null); } catch (e) { setError(e instanceof Error ? e.message : '删除事件失败'); } }
  return <div className="shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">V</span><div><strong>VISION OPS</strong><small>EVENT WORKSPACE</small></div></div><nav>{['事件检索','视频任务','规则配置','模型版本'].map(item => <button key={item} className={activeNav === item ? 'active' : ''} onClick={() => setActiveNav(item)}>{item}</button>)}</nav><div className="system"><span className="dot" />系统在线<small>API · WORKER · STORAGE</small></div></aside><main className="content"><header><div><p className="eyebrow">视觉事件运营平台 / PHASE 02</p><h1>视频事件检索</h1></div></header>{error && <div className="notice" role="alert">{error}</div>}{feedback && <div className="feedback" role="status">{feedback}</div>}{activeNav === '事件检索' ? <section className="workspace"><div className="event-column"><div className="section-head"><div><p className="eyebrow">EVENT STREAM</p><h2>事件流</h2></div><div className="search"><input aria-label="搜索事件" placeholder="搜索事件类型…" value={keyword} onChange={e => setKeyword(e.target.value)} /></div></div><div className="rule-strip">规则引擎：{rules.map(r => `${r.event_type} · ${r.version}`).join(' / ') || '加载中'}</div>{groups.length ? <div className="event-list">{groups.map(group => { const event = group.events[0]; return <button className={`event-card ${selected?.id === event.id ? 'selected' : ''}`} key={group.key} onClick={() => choose(event)}>{event.evidence.thumbnail_url ? <img className="event-thumb image-thumb" src={event.evidence.thumbnail_url} alt="事件证据缩略图" /> : <div className="event-thumb">◉<i>{time(event.start_time_ms)}</i></div>}<div className="event-copy"><div><strong>{displayEventType(event.event_type)}</strong><span className={`severity ${event.severity}`}>{event.severity}</span></div><p>{(event.analysis ?? fallbackAnalysis(event)).summary}</p><small>{detectionSummary(event)} · {event.rule_version ?? 'rule-v1'}{group.events.length > 1 ? ` · ${group.events.length} 个片段` : ''}</small><em>{event.event_type}</em></div></button>; })}</div> : <div className="empty">暂无匹配事件</div>}</div><div className="detail-column">{selected ? <><div className="detail-top"><div><p className="eyebrow">EVENT DETAIL / {selected.id}</p><h2>事件详情</h2></div><span className="status">{statusLabel(selected.status)}</span></div><div className="evidence">{active ? <><div className="evidence-stage"><img src={active.image_url} alt={`${preciseTime(active.timestamp_ms)} 的检测证据`} />{active.detections.map((d, i) => <span className="detection-box" key={i} style={box(d.bbox)}>{d.class_name} {(d.confidence * 100).toFixed(0)}%</span>)}</div><div className="timeline">{frames.map((f, i) => <button key={f.image_url} aria-label={`证据帧 ${preciseTime(f.timestamp_ms)}`} aria-pressed={i === frame} onClick={() => setFrame(i)}>{preciseTime(f.timestamp_ms)}</button>)}</div></> : <div className="evidence-empty">暂无可用抽帧证据</div>}</div><div className="detail-info"><div><span>事件类型</span><strong>{displayEventType(selected.event_type)}</strong><small>{selected.event_type} · {selected.rule_version ?? 'rule-v1'}</small></div><div><span>检测摘要</span><strong>{detectionSummary(selected)}</strong></div><div><span>模型置信度</span><strong>{(selected.confidence * 100).toFixed(1)}%</strong></div></div><div className="analysis"><p className="eyebrow">MODEL ANALYSIS</p><h3>{analysis?.summary}</h3><p>{analysis?.suggestion}</p><small>来源：{analysis?.report_source} · 检测器：{selected.detector_version}</small></div><div className="actions"><button className="confirm" onClick={() => review('confirm')} disabled={reviewing || selected.status === 'confirmed'}>{reviewing ? '保存中…' : '确认事件'}</button><button onClick={() => review('ignore')} disabled={reviewing || selected.status === 'ignored'}>{selected.status === 'ignored' ? '已忽略' : '忽略'}</button><button className="danger-button" aria-label={`删除事件 ${selected.event_type}`} onClick={() => setDeleting(selected)}>删除事件</button></div></> : <div className="empty detail-empty">选择一个事件查看证据与分析</div>}</div></section> : activeNav === '视频任务' ? <JobsPage jobs={jobs} onRefresh={async () => setJobs(await api.listJobs())} /> : activeNav === '规则配置' ? <RulesPage rules={rules} onSaved={async () => setRules(await api.listRules())} /> : <ModelsPage />}{deleting && <div className="modal-backdrop"><div className="modal" role="dialog" aria-modal="true" aria-labelledby="delete-event-title"><h3 id="delete-event-title">确认删除事件？</h3><p>事件“{deleting.event_type}”将被永久删除，原视频不会受到影响。</p><div className="modal-actions"><button onClick={() => setDeleting(null)}>取消</button><button className="danger-button" onClick={remove}>确认删除</button></div></div></div>}</main></div>;
}
